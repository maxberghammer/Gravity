<UserControl x:Class="Gravity.Wpf.View.Direct3dWorldView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:hx="http://helix-toolkit.org/wpf/SharpDX"
             xmlns:core="clr-namespace:HelixToolkit.SharpDX.Core;assembly=HelixToolkit.SharpDX.Core"
             xmlns:system="clr-namespace:System;assembly=System.Runtime"
             mc:Ignorable="d"
             d:DesignHeight="450" 
             d:DesignWidth="800">
    <Grid HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch">
        <hx:Viewport3DX Name="mViewport3Dx"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        BackgroundColor="Black"
                        ShowFrameDetails="True"
                        ShowTriangleCountInfo="True"
                        ShowViewCube="False"
                        ShowFrameRate="True"
                        IsZoomEnabled="False"
                        IsRotationEnabled="True"
                        InfoBackground="Transparent"
                        InfoForeground="Red"
                        MSAA="Maximum"
                        SizeChanged="OnSizeChanged"
                        DataContextChanged="OnDataContextChanged">
            <hx:Viewport3DX.Camera>
                <hx:PerspectiveCamera x:Name="mCamera" 
                                      NearPlaneDistance="{x:Static system:Double.NegativeInfinity}"
                                      FarPlaneDistance="{x:Static system:Double.PositiveInfinity}" />
            </hx:Viewport3DX.Camera>
            <hx:Viewport3DX.EffectsManager>
                <core:DefaultEffectsManager/>
            </hx:Viewport3DX.EffectsManager>
            <hx:DirectionalLight3D Color="White"
                                   Direction="0 0 -1"/>
        </hx:Viewport3DX>
        <Button HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Click="ButtonBase_OnClick">
            Test
        </Button>
    </Grid>
</UserControl>